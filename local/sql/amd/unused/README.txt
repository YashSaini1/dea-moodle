This folder needs to store unused JS code.
The main reason is that moodle concat all compiled js files to the first.min.js and send it to the user.
If we store many unused files, the users page loaded slowly.